import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import { Input } from '@code-x/input';
import Fieldset from './Fieldset';

<Meta title="Form Components/Fieldset" component={Fieldset} />

# Fieldset

A **Fieldset** is a visual wrapper for multiple related form elements.

<Canvas>
  <Story name="Example">
    {(args) => (
      <Fieldset label="Example Fieldset">
        <Input label="Input 1" name="input1" />
        <Input label="Input 2" name="input2" />
      </Fieldset>
    )}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Example" />
