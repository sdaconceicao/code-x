export default ({ palette, form }) => ({
  button: {
    padding: 5,
    borderRadius: form.borderRadius
  },
  primary: {
    color: palette.primary
  },
  secondary: {
    color: palette.success
  }
});
