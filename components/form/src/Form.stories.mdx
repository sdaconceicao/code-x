import { useState } from 'react';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { FullExample } from './Form.template.jsx';
import { Form } from './Form.jsx';

<Meta
  title="Form Components/Form"
  component={Form}
  parameters={{
    actions: { argTypesRegex: '^handle.*' }
  }}
/>

# Form

An **Form** wraps Form Elements and connects them to the **Form Context**. Using an
onSubmit callback, results from the form including validation can be retrieved.

<Story name="Example">
  <FullExample />
</Story>

## Properties

<ArgsTable story="Example" />
