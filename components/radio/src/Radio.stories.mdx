import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import RadioFormElement, { RadioComponent } from './Radio';

<Meta title="Form Components/Radio" component={RadioComponent} />

# Radio

A Radio is an html form element for controlling boolean values

<Canvas>
  <Story name="Example">
    {(args) => (
      <RadioFormElement
        name="example"
        id="example"
        label="Label"
        value="Lorem ipsum"
        onChange={action('value changed')}
        {...args}
      />
    )}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Example" />
