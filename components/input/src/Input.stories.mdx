import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import InputFormElement, { InputComponent } from './Input';

<Meta title="Form Components/Input" component={InputComponent} />

# Input

An **Input** is an html form element where the user can enter text data.

<Canvas>
  <Story name="Example">
    {(args) => (
      <InputFormElement
        name="example"
        id="example"
        label="Label"
        type="text"
        value="Lorem ipsum"
        label="Lorem"
        onChange={action('value changed')}
        {...args}
      />
    )}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Example" />
