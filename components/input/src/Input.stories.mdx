import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import theme from '@code-x/theme';
import { ThemeProvider } from 'react-jss';
import { Input } from '.';


<Meta title="Form Components/Input" component={Input} />

# Input
An input is an html form element

<Canvas>
  <Story name="Example">
    {args => (
      <Input name="example" 
        id="example"
        label="Label" 
        type="text"
        value="Lorem ipsum"
        onChange={action('value changed')}
        {...args}
        />
    )}
  </Story>
</Canvas>

## Properties
<ArgsTable story="Example" />
