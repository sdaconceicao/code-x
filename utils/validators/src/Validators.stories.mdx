import { useState } from 'react';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

<Meta title="Utilities/Validators" />

# Validators

A validator is a function that takes in a object containing values and configuration for en element, and an object
containing if the data passes or any errors for the user to correct.
